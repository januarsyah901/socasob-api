meta {
  name: 6. Delete User (DELETE)
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/admin/users/60d5ec49f1b2c80015f8e4a1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: in [200, 401, 403, 404]
}

tests {
  test("delete user admin handled", function() {
    expect([200, 401, 403, 404]).to.include(res.getStatus());
  });
}
