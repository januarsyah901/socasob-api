meta {
  name: Delete Medical Report (DELETE)
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/reports/SOCA-999999
  body: none
  auth: none
}

assert {
  res.status: in [200, 404]
  res.body.success: isDefined
}

tests {
  test("delete medical report handled", function() {
    expect([200, 404]).to.include(res.getStatus());
  });
}
