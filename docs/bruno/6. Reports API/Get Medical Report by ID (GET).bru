meta {
  name: Get Medical Report by ID (GET)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/reports/SOCA-882104
  body: none
  auth: none
}

assert {
  res.status: in [200, 404]
  res.body.success: isDefined
}

tests {
  test("get medical report by id handled", function() {
    expect([200, 404]).to.include(res.getStatus());
  });
}
