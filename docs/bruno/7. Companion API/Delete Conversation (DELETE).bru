meta {
  name: Delete Conversation (DELETE)
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/companion/conversations/conv-12345
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: in [200, 404]
  res.body.success: isDefined
}

tests {
  test("delete conversation handled", function() {
    expect([200, 404]).to.include(res.getStatus());
  });
}
