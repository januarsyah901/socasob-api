meta {
  name: 6. Delete Robot (DELETE)
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/robots/robot-demo-01
  body: none
  auth: none
}

assert {
  res.status: in [200, 404]
  res.body.success: isDefined
}

tests {
  test("delete robot handled", function() {
    expect([200, 404]).to.include(res.getStatus());
  });
}
